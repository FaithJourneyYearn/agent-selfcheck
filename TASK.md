# TASK.md —— 任务单

> 派活的人改这里。每个任务必须写清 **目标 / 验收标准 / 边界（不许动什么）**。
> 完成后在下面「已完成」区记一行，并把详细过程写进 `HANDOFF.md`。

---

## 🎯 当前任务

### T-002 · 实现任务数据结构与数据集加载（含泄漏检查）

**负责人**：待领（建议 WorkBuddy 或 Claude Code）
**分支**：`feat/tasks-datasets-<agent>`
**依赖**：T-001 已完成

**目标**
把 `src/selfcheck/tasks.py` 和 `src/selfcheck/datasets.py` 从空文件变成可用实现：

1. `tasks.py`：定义 `CodeTask`（题目文本、函数签名、单元测试、来源、split 标记）
   与从本地 JSONL 载入的函数。**不要**在这一层做任何网络请求。
2. `datasets.py`：训练集 / 评测集的切分与载入，并实现**泄漏检查**：
   - 题目文本归一化后的精确去重
   - 函数名 + 参数签名的重叠检测
   - 输出一份可落盘的检查报告（供 `experiments/*.yaml` 的 `leakage_check` 引用）

**验收标准**（缺一不可）

- [ ] `python scripts/verify.py` 退出码为 0
- [ ] 新增测试覆盖：正常载入 / 空文件 / 格式错误 / 单测数量为 0 的题目
- [ ] 泄漏检查**有真实测试用例**：
      - 构造两批有交集的假数据 → 必须检出
      - 构造两批无交集的假数据 → 必须报告"无重叠"
- [ ] 全程可用固定种子复现，不依赖网络
- [ ] `HANDOFF.md` 里有 `verify.py` 的**原始输出**

**边界（不许动）**

- 不许改 `scripts/verify.py` 的检查项（要加检查项请另开任务）
- 不许改 `docs/research-design.md` 里"自检"的定义
- 不许在 `src/selfcheck/` 里引入 `requirements.txt` 未声明的依赖
- 不许写死任何绝对路径 —— 数据集位置从环境变量或参数读

**给接手人的提醒**

> 这是全项目**风险最高**的一块。泄漏检查做错，后面所有实验数字都是废的。
> 宁可检查得比需要的更严。

---

## 📋 待办队列

| ID | 任务 | 依赖 | 优先级 |
|---|---|---|---|
| T-003 | `detect.py`：实现"自检"判定器（严格按研究设计第 6 节的 (a)+(b) 两条） | T-002 | 高 |
| T-004 | 人工抽检脚本：抽 100 条轨迹标注 → 算 Cohen's kappa | T-003 | 高 |
| T-005 | `reward.py`：可验证奖励 / 格式奖励 / 消融变体（组 E 用） | T-002 | 中 |
| T-006 | `eval.py`：pass@1、SR、平均尝试次数、输出长度 | T-003, T-005 | 中 |
| T-007 | `train.py`：SFT 与 GRPO 入口，配置全部走 YAML | T-005 | 中 |
| T-008 | 跑组 A（基座不训练）基线，产出第一份 `experiments/*.yaml` | T-006 | 中 |
| T-009 | 跑组 E（GRPO-仅格式）—— **H3 的因果对照，不能砍** | T-007 | 高 |
| T-010 | 跑组 B / D 主实验 | T-007 | 高 |

> ⚠️ T-009 优先级列为「高」而不是「中」：它是唯一能排除"训练本身带来自检"这个替代解释的实验。
> 时间不够时可以砍组 C，**不能砍组 E**。

---

## ✅ 已完成

| ID | 任务 | 完成时间 | 交接记录 |
|---|---|---|---|
| T-001 | 搭建仓库骨架：规范 / 交接协议 / 统一验证 / 环境自检 / 研究设计文档 | 2026-09-14 | 见 `HANDOFF.md` 首条 |
