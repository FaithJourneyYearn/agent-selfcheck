# 项目约定（唯一事实源）

> **三个 agent 都读这个文件。**
> 规则只在这里改。`AGENTS.md` 和 `CLAUDE.md` 只是入口指针，不复制内容 —— 复制多份必然漂移。

---

## 1. 项目是什么

- **名称**：`agent-selfcheck` —— 代码 Agent 自检行为的可验证奖励研究
- **一句话目标**：验证「自检行为能否通过可验证奖励（单元测试通过率）被强化学习诱导出来」，并与 SFT 监督基线对比
- **使用者是谁**：
  1. 我自己（杭电人工智能专业，用于本科毕设 / 考研复试材料）
  2. 审阅者（复试老师、导师）—— 他们只看 README、研究设计、实验记录三样东西
- **完成的标准是什么**（可被追问，不是"做好了"）：
  1. `docs/research-design.md` 里的 H1 / H2 / H3 三条假设，每条都有对应的实验结果支撑或否证
  2. 每个结论都能用 `experiments/` 下的记录重新跑出来（配置完整、种子固定）
  3. 主结论带 **基线对比 + 样本量 + 标准差**，且通过了消融实验（组 E）
  4. 数据泄漏检查有留痕，且"自检"判定的人工抽检一致率有报告
  5. 能对着 README 讲 10 分钟而不被问倒

---

## 2. 技术栈与运行方式

- **语言 / 版本**：Python 3.13（虚拟环境）
  - 建环境：`python -m venv .venv`，激活后 `pip install -r requirements.txt`
- **依赖管理**：`requirements.txt`，原则是**跑不起来之前不要装**
- **怎么跑起来**（骨架阶段）：
  ```
  python -m selfcheck.env_check
  ```
- **怎么跑测试 / 验证**：
  ```
  python scripts/verify.py
  ```
- **实验阶段（尚未开始）**：训练 / 评测入口在 `src/selfcheck/train.py` 与 `eval.py`，
  **目前是空占位**，实现前先在 `TASK.md` 立案。

---

## 3. 目录结构

| 目录 | 放什么 | 谁负责 |
|---|---|---|
| `src/selfcheck/` | 源码：任务、数据、奖励、自检判定、训练、评测 | 谁改谁负责 |
| `tests/` | 测试。**必须存在**，见第 5 条 | 全部 |
| `scripts/` | 工具脚本（验证入口等） | 通用 |
| `docs/` | 研究设计、决策记录 | 谁做决策谁写 |
| `docs/decisions/` | 重大取舍，一条一个文件 | 谁做决策谁写 |
| `experiments/` | 实验记录 YAML，**只追加不改** | 跑实验的人 |
| `TASK.md` | 当前任务单 | 派活的人 |
| `HANDOFF.md` | 交接日志（只追加） | 每个 agent 干完都写 |

---

## 4. 交接协议

**不要两个 agent 同时改同一个文件。**

```
1. 派活：改 TASK.md，写清楚「目标 / 验收标准 / 边界（不许动什么）」
2. 领取：git checkout -b feat/<任务>-<agent 名>
3. 干活：改代码，同时改 tests/
4. 自检：跑 scripts/verify.py；有图表就截图
5. 交接：git commit → 在 HANDOFF.md 追加一条（含 commit hash）
6. 复审：下一个 agent 读 HANDOFF.md，跑 verify.py 复现
```

**共享的是 diff，不是文件。** 三个 agent 共用一个 git 仓库，各自开分支；审阅通过才合入 `main`。

---

## 5. 验证标准（**所有 agent 必须遵守，不许例外**）

1. **不许声称未验证的结果。** 说"修好了"必须同时贴出原始命令输出。
2. **任何代码改动后必须跑 `python scripts/verify.py`。** 退出码非 0 就不算完成。
3. **实验数字必须有口径。** 任何"提升了 X%"必须说明：对比基线、重复次数、随机种子、是单次还是均值。
4. **涉及图表的改动必须截图。** 渲染 → 贴出 PNG 绝对路径。没有截图等于没做。
5. **新增功能必须新增测试。** 只加功能不加测试的改动直接打回。
6. **实验记录只追加。** `experiments/*.yaml` 一旦写入不许改；要重跑就新建一个文件并注明与哪个对比。
7. **不确定就说不确定。** 编造 API、编造实验数字、假称"已测试通过"是最严重的错误。

---

## 6. 研究专属红线（比一般工程约定更严）

- **数据泄漏**：训练集与评测集必须做重叠检查并留痕。这是这类实验的头号死因。
- **定义前置**：`docs/research-design.md` 第 6 节对"自检"的操作化定义**一经记录不得放宽**。
  若确实需要修改，必须在 `docs/decisions/` 留下决策记录，并说明修改前的结论是否仍成立。
- **负结果照实报**。假设被否证同样是有效结论，不许只报好消息。
- **禁止事后拟合**：不得在看到结果后才补一个"其实我一开始就是这个意思"的定义。

---

## 7. 代码规范

- 命名：模块/函数用下划线，类用驼峰，常量全大写
- 注释：只写"为什么这么写"，不解释"这行在干嘛"
- 错误处理：宁可抛异常，不要静默返回默认值
- 提交信息：`<type>: <做了什么>`，type ∈ feat / fix / refactor / docs / exp / test
  （`exp:` 用于实验相关改动，便于日后筛出实验历史）

---

## 8. 禁止事项

- 不要引入未在 `requirements.txt` 声明的依赖
- 不要重构与当前任务无关的代码
- 不要删除 `tests/` 里的既有用例来让测试通过
- 不要提交模型权重、数据集原文、密钥、个人路径

---

## 9. 决策记录

重大取舍写进 `docs/decisions/`，一条一个文件：
`YYYY-MM-DD-<决策>.md` → 背景 / 选项 / 选了什么 / 为什么 / **什么情况下该反悔**。
最后那一栏最重要 —— 它让未来的你知道自己当初的假设是什么。
