# 依赖清单 —— 只声明真正需要的。
# 原则：跑不起来之前不要装。每加一个依赖，在 HANDOFF.md 里说明为什么。

# --- 当前阶段（搭骨架、跑验证）---
pytest>=8.0

# --- 实验阶段：真正开始跑训练/评测时再解开 ---
# torch>=2.4
# transformers>=4.44
# trl>=0.12          # GRPO / SFT trainer
# datasets>=2.20
# vllm>=0.6          # 采样加速
# numpy>=1.26
# scipy>=1.13        # 统计检验（配对 t 检验 / bootstrap）
